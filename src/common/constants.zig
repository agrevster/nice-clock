/// Used to determine how often the clock's screen should be updated.
pub const fps: u8 = 60;

///The amount of modules that have to run before the clock reloads `config.luau`, potently changing the loaded modules.
pub const config_reload_interval: u8 = 5;
